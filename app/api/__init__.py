"""HTTP 경계 — BE·인프라 담당이 검토할 범위는 이 패키지가 전부다.

여기 밖의 코드는 모델·런타임 구현이라 와이어 계약을 바꾸지 못한다. 자세한
안내는 `app/api/README.md` 를 본다.

- `routers/` — 엔드포인트와 상태 코드
- `schemas/` — 요청·응답 본문
- `errors.py` — 예외 → 상태 코드 표
- `deps.py` — 인증과 런타임 주입
"""
