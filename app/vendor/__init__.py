"""모델 레포에서 원본 그대로 가져온 코드.

`rgb_ext_infer.py` 는 `kt-aivle-big-proj-model-rgb` 의 `ext_infer.py` 를 해시
그대로 옮긴 파일이다. 이 패키지 안에서는 원본을 수정하지 않는다. 서버가 필요로
하는 각색은 같은 폴더의 `rgb_ext_infer_onnx.py` 처럼 별도 모듈에서 한다.
"""
