# DB 권고사항

## RGB 결함 유형 저장

현재 RGB 결함 어댑터는 모델의 실제 출력값을 변환하지 않고
`defects[].defectType`에 저장한다.

```text
녹·부식
벗겨짐·박리
파손·찢김
긁힘·스크래치
들뜸
오염·이물질
```

DB와 BE에는 다음 조치를 권고한다.

1. `defectType` 저장 컬럼이 문자열인지 확인한다.
2. PostgreSQL enum이나 CHECK 제약으로 `CRACK`, `SPOT`만 허용한다면 위
   6종을 저장할 수 있도록 마이그레이션한다.
3. BE의 DTO 또는 Java enum도 위 6종을 수신할 수 있도록 수정한다.
4. 한글 값을 장기 계약으로 확정할지, 영문 코드로 바꿀지는 별도 결정으로
   남긴다. 영문 코드가 확정되기 전까지 DB에서 임의 변환하지 않는다.
5. 기존 `CRACK`, `SPOT` 데이터가 있다면 삭제하거나 덮어쓰지 말고 신규
   값과 함께 조회 가능한 전환 계획을 마련한다.

이 권고가 반영되지 않은 상태에서는 RGB 결함 응답이 BE 파싱 또는 DB 저장
단계에서 실패할 수 있다.
